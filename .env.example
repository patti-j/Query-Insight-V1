# App
NODE_ENV=production
PORT=5000

# OpenAI Configuration
AI_INTEGRATIONS_OPENAI_API_KEY=your_openai_api_key_here
# Optional: Override base URL if needed
# AI_INTEGRATIONS_OPENAI_BASE_URL=https://api.openai.com/v1

# ============================================================================
# Database Configuration (Azure SQL)
# ============================================================================
# RECOMMENDED: Use a single connection string
DATABASE_URL=Server=tcp:your-server.database.windows.net,1433;Initial Catalog=your-database;Persist Security Info=False;User ID=your-user;Password=your-password;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;

# ALTERNATIVE: Use discrete secrets (backward compatible)
# If DATABASE_URL is not set, these will be used instead
# SQL_SERVER=your-server.database.windows.net
# SQL_DATABASE=your-database-name
# SQL_USER=your-username
# SQL_PASSWORD=your-password

# Query Insight Configuration
DASHT_PLANNING_TABLE=[publish].[DASHt_Planning]
