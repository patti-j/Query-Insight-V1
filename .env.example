# App
NODE_ENV=production
PORT=5000

# OpenAI
AI_INTEGRATIONS_OPENAI_API_KEY=your_key_here
# Optional: Override base URL if needed
# AI_INTEGRATIONS_OPENAI_BASE_URL=https://api.openai.com/v1

# Azure SQL (recommended pattern: one connection string or discrete parts)
# Option A: discrete parts
SQL_SERVER=your-server-name.database.windows.net
SQL_DATABASE=your-db-name
SQL_USER=your-user
SQL_PASSWORD=your-password
# Optional: AZURE_SQL_* aliases are also supported by the code

# Option B: single connection string (if your code supports it)
# AZURE_SQL_CONNECTION_STRING=...

# Query Insight target
DASHT_PLANNING_TABLE=[publish].[DASHt_Planning]
